# The Great Discovery

A pressure-driven autonomous discovery engine.

## Run

```bash
python run_discovery.py
```

## Architecture

Runner → Engine → Discovery Modules

Core modules live in `great_discovery/`.

Runtime artifacts such as databases and logs are ignored via `.gitignore`.
